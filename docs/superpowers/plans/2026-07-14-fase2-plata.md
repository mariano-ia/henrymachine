# Fase 2 — Plata (upsell · cupones · regalo · leads) · Implementation Plan

> Ejecutada por Opus en la sesión (no subagentes), con verificación por task.
> Prerrequisito: Fases 0 y 1 completas. Leer el bloque **Contexto operativo** del
> plan de Fase 0 — aplica entero (dev server con pkill, migraciones con
> `scripts/db-run.mjs`, tsc, Stripe LIVE = no completar compras, voz peruana en
> el chat / voseo en UI, commits con Co-Authored-By, no deployar).

**Goal:** Cerrar el circuito de monetización: vender la SIGUIENTE experiencia al
terminar (con cupón), gestionar cupones sobre Stripe, permitir regalar un
recorrido, y capturar emails de los que no compran hoy.

**Architecture:** Stripe es la única fuente de verdad de cupones (Coupons +
Promotion Codes vía API; sin tabla local de redenciones). El upsell y el regalo
son variantes del checkout ya existente. Los leads viven en una tabla propia con
consentimiento.

## Estado real descubierto (2026-07-14)

- `entitlement_source` = `purchase | grant | free`. Un regalo = entitlement
  `source='grant'` con `grant_email` = email del regalado (NO el anon del comprador).
- `entitlements` tiene índice único `entitlements_email_uq (experience_id, grant_email)`
  → regalar algo que el email ya tiene choca 23505 → ignorar (ya lo tiene).
- `purchases` NO tiene columnas de regalo. `experiences` NO tiene columnas de upsell.
- No existen tablas coupons/leads.
- El webhook (`app/api/stripe/webhook/route.ts`) ya crea entitlement por
  `checkout.session.completed` usando `anon_id` + `grant_email`. Para regalo hay
  que dirigir el entitlement al email del regalado, leído de metadata.

---

### Task 1: Migración 0011 — upsell, regalo, leads

**Files:** Create `supabase/migrations/0011_plata.sql`; modify `lib/database.types.ts`.

SQL:
```sql
-- 0011: monetización — upsell por experiencia, regalo, leads.

-- Upsell: al terminar una experiencia, ofrecer la siguiente (con cupón opcional).
alter table experiences add column if not exists upsell_experience_id uuid references experiences(id) on delete set null;
alter table experiences add column if not exists upsell_message text;
alter table experiences add column if not exists upsell_promo_code text; -- Promotion Code de Stripe (el código visible, ej. GOLAZO20)

-- Regalo: la compra es para otra persona (el entitlement va al email del regalado).
alter table purchases add column if not exists is_gift boolean not null default false;
alter table purchases add column if not exists gift_recipient_email citext;
alter table purchases add column if not exists gift_message text;

-- Leads: emails de gente que todavía no compró (avisar de nuevos recorridos).
create table if not exists leads (
  id uuid primary key default gen_random_uuid(),
  email citext not null,
  source text,                 -- home | paywall | ...
  slug text,                   -- experiencia desde donde se captó (si aplica)
  marketing_consent boolean not null default true,
  created_at timestamptz not null default now()
);
create unique index if not exists leads_email_uq on leads (email);
alter table leads enable row level security;
-- sin policies: escribe/lee solo el service role

notify pgrst, 'reload schema';
```

Verificación: aplicar; consultar que existen las columnas y la tabla `leads`.
Types: agregar upsell_* a experiences, is_gift/gift_* a purchases, y la tabla leads.

### Task 2: Cupones sobre Stripe (lib + admin)

**Files:** Create `lib/stripe-coupons.ts`; `app/admin/(app)/cupones/page.tsx`;
`app/admin/(app)/cupones/actions.ts`; `components/admin/CouponsEditor.tsx`; link en el nav admin.

- `lib/stripe-coupons.ts`: `listPromotionCodes()`, `createCoupon({code, percentOff|amountOff, maxRedemptions, expiresAt})` (crea Stripe Coupon + Promotion Code), `setPromotionActive(id, active)`.
- Admin `/admin/cupones`: form de alta (código, % o monto USD, usos máx, vencimiento) + lista con estado (activo, usos, vencimiento) + toggle activar/desactivar. Stripe = fuente de verdad (se lista desde Stripe).
- ⚠️ Stripe LIVE: crear cupones es seguro (no cobra), pero real. Verificar creando UN cupón de test, listándolo y desactivándolo.

### Task 3: Config de upsell en el editor de experiencias

**Files:** modify `components/admin/ExperienceEditor.tsx` + `app/admin/(app)/e/[id]/actions.ts` (saveExperience) + `app/admin/(app)/e/[id]/page.tsx` (select *).

- Sección "Al terminar" en el editor: elegir experiencia siguiente (select de las publicadas), mensaje de Henry, y código de cupón (texto libre — el Promotion Code de Stripe).
- saveExperience persiste upsell_experience_id / upsell_message / upsell_promo_code.

### Task 4: Oferta post-final en el chat

**Files:** `lib/db/experiences.ts` (getPlayableExperience expone el upsell), `app/api/experience/route.ts` (lo pasa client-safe), `components/PlayerLoader.tsx` + `components/PlayerChat.tsx` (card de upsell en el estado TERMINADO).

- Al TERMINADO, si la experiencia tiene upsell configurado, Henry muestra una card
  con la siguiente experiencia (título, cover, precio con descuento si hay cupón) y
  un CTA que va a `/e/<slug-upsell>` (o checkout directo con el promo aplicado).
- Voz de Henry en el mensaje del upsell.

### Task 5: Regalar un recorrido

**Files:** `app/api/checkout/route.ts` (modo regalo), `app/api/stripe/webhook/route.ts`
(entitlement al regalado), UI de "regalar" en el detalle (`components/BuyBar.tsx` o
un componente nuevo), y la nota amarilla como tarjeta de regalo.

- Checkout con `{ gift: true, recipientEmail }`: la purchase marca is_gift +
  gift_recipient_email; el webhook crea el entitlement con source='grant' +
  grant_email = regalado (NO anon del comprador). 23505 → ignorar.
- El comprador recibe la "nota amarilla de Henry" como tarjeta compartible.

### Task 6: Captura de leads

**Files:** `app/api/lead/route.ts`, un componente `components/LeadCapture.tsx`,
insertado en la home y en el paywall del chat gratis.

- POST /api/lead {email, source, slug, consent} → upsert en leads (onConflict email).
- Form chico "avisame del próximo recorrido" en home + paywall.

## Fuera de esta fase
- 2.7 Emails transaccionales: BLOQUEADO por dominio (Resend). Diseño ya en el spec.
- La reseña post-final (parte de 2.4 en el spec) es Fase 3.
